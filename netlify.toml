[build]
  command = "rm -rf node_modules package-lock.json && npm install && npm run build"
  publish = "dist/client"

[build.environment]
  ROLLUP_DISABLE_NATIVE = "true"

[dev]
  command = "vite dev"
  port = 8080
